# Advanced / compatibility profile only. Explicit opt-in to Fast for the whole session.
# On current Codex builds, child agents follow the session tier; this is not Luna-only Fast.

service_tier = "fast"

[features]
multi_agent = true
