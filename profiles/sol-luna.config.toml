# Advanced / compatibility profile only. Daily use is: install once, then `codex`.
# Leaves Root model, reasoning, service tier, and Fast choice to the user/session.
# Luna role routing and fixed child settings come from the installed csl_luna_* roles.

[features]
multi_agent = true
